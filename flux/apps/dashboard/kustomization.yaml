apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dashboard
resources:
  - namespace.yaml
  - secret.yaml
  - glance.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: glance-config
    files:
      - glance.yaml=glance-config.yaml 