pages:
  - name: Home
    columns:
      - size: small
        widgets:
          widgets:
          - type: calendar
          - type: weather
            location: ${CITY} 
            units: metric
            hour-format: 24h