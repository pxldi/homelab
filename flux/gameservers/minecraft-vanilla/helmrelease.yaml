apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minecraft
  namespace: minecraft
spec:
  interval: 30m
  chart:
    spec:
      chart: minecraft
      version: '5.0.0'
      sourceRef:
        kind: HelmRepository
        name: minecraft-server
        namespace: minecraft
  valuesFrom:
    - kind: ConfigMap
      name: minecraft-values
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3